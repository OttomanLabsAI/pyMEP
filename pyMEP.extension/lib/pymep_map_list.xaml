<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Map names"
        Width="600" Height="620" MinWidth="520" MinHeight="460"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Background="White">
    <Window.Resources>
        <Style x:Key="Hint" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F4F4F4" Padding="14,10">
            <StackPanel>
                <TextBlock FontSize="15" FontWeight="SemiBold"
                           Text="Map file names to this model"/>
                <TextBlock Style="{StaticResource Hint}"
                           Text="Only names WITHOUT an exact match in this model are listed (matching names map to themselves). '(import as is)' brings the item in as new; picking a model name redirects every reference to it and skips importing the file's own copy."/>
            </StackPanel>
        </Border>

        <TextBox x:Name="TxtSearch" Grid.Row="1" Height="24"
                 Margin="14,10,14,4" VerticalContentAlignment="Center"
                 TextChanged="on_search"/>

        <Border Grid.Row="2" Margin="14,4,14,6" BorderThickness="1"
                BorderBrush="#DDDDDD">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="PnlRows" Margin="8,6,8,6"/>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Background="#F4F4F4" Padding="14,10">
            <Grid>
                <TextBlock x:Name="TxtCount" VerticalAlignment="Center"
                           Text=""/>
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="BtnOk" Content="Use this mapping"
                            Style="{StaticResource DialogButton}"
                            IsDefault="True" Click="on_ok"/>
                    <Button x:Name="BtnCancel" Content="Cancel"
                            Style="{StaticResource DialogButton}"
                            IsCancel="True" Click="on_cancel"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
